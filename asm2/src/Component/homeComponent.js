import React from 'react';
import { IMG } from '../shared/staffs'
function Home(props) {
    return (
        <div>
            <img src={IMG.image} alt={"anh"} />
        </div>

    );
}

export default Home;